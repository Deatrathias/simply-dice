{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Debug chrome",
      "url": "http://localhost:30001",
      "webRoot": "${workspaceFolder}"
    }
  ]
}